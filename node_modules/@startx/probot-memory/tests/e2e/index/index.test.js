"use strict";
var memory = require("../../../src/index");
const Should = require('should');
describe("[e2e] entire package", function () {
    it("should be able to get the LongTermMemory instance", function (done) {
        Should(memory.LongTermMemory).be.an.Object();
        done();
    });
});
