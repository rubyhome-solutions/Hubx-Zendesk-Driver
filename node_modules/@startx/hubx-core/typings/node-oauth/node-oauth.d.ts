declare namespace oAuthInstance {
	export class OAuth2 {
    constructor(clientId: string, clientSecret: string, baseSite: string, authorizePath?: string, accessTokenPath?: string, customHeaders?: string);
		getOAuthAccessToken(code: string, params: Object, callback: (e, access_token, refresh_token, results) => void);
  }
}
