"use strict";
var config = require("../../../src/singletons/config/config");
const oauth_1 = require("../../../src/protocols/oauth/oauth");
var Should = require("should");
describe("[unit] protocols -> oauth", function () {
    before(function (done) {
        this.oauth = new oauth_1.oAuth();
        done();
    });
    it("should be able to set property", function (done) {
        Should(this.oauth).have.property("createAccount");
        Should(this.oauth.createAccount).be.a.Function();
        done();
    });
});
